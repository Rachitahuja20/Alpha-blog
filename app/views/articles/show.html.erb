
<h2 align="center"> Article no. <%= @article.id %> : <%= @article.title %> </h2>

<div class="well col-xs-8 col-xs-offest-2">
  <h4 class="center description"><strong>Description:</strong></h4>
  <hr>
   <%= simple_format(@article.description) %>
  
  <div class="article-actions">
    <%= link_to "Edit", edit_article_path(@article), class: "btn btn-xs btn-primary" %>
    <%= link_to "Show all articles", articles_path, class: "btn btn-xs btn-success" %>
    <%= link_to "Delete", article_path(@article), class: "btn btn-xs btn-danger", method: :delete, data: {confirm: "Are you sure"} %></td>
  </div>
</div>